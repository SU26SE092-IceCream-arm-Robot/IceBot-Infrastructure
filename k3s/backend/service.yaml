apiVersion: v1
kind: Service
metadata:
  name: icebot-backend
  namespace: icebot

spec:
  selector:
    app: icebot-backend

  ports:
    - name: http
      port: 8080
      targetPort: 8080

  type: ClusterIP
